#!/bin/sh
set -eu
if [ "$1" = configure ] && [ -x /sbin/apparmor_parser ] && [ -d /sys/kernel/security/apparmor ]; then
  /sbin/apparmor_parser -r /etc/apparmor.d/pipeline-desk
fi
if command -v update-desktop-database >/dev/null 2>&1; then
  update-desktop-database -q /usr/share/applications
fi
